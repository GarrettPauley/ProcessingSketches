/*
Mover[] movers = new Mover[20]; 
void setup(){ 
  size(640,480); 
  //initialize the Mover objects
  for (int i = 0; i < movers.length; i++){ 
    movers[i] = new Mover(); 
  }
}

void draw(){ 
  background(255); // draw white background on every frame to get rid of the trail. 
  for(int i = 0; i<movers.length; i++){
    
    movers[i].update(); 
    movers[i].checkEdges(); 
    movers[i].display(); 
  }
  
  
}

*/
